# URL of your deployed Aroha proxy (Cloudflare Worker).
# The Gemini API key is NOT here — it lives only in the worker (see SCAFFOLD_SETUP.md).
EXPO_PUBLIC_AROHA_API_URL=https://aroha-proxy.your-subdomain.workers.dev
